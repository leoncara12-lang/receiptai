services:
  - type: web
    name: receiptai-single
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: RECEIPT_SIGNING_SECRET
        generateValue: true
